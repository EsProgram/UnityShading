MVP変換行列をスクリプトからシェーダーに設定するサンプル。
複数のオブジェクトにsharedMaterialを適用すると、特定条件下でDynamicBatchingさ
れるので、Batchingされたオブジェクトは同じ変換行列を使用することになるので表示
が変になる。
普通にオブジェクトごとの変換行列を利用するなら、UNITY_MATRIX_MVPを利用するべ
き。ただ、特定のオブジェクトやカメラから変換行列を取り出してシェーダーで使うの
はよくあることなので、この手法はマスターしておいたほうが良い。
